import { BaseClient } from '../core/client';
import { Config } from '../core/config';
import { SearchRequest, SearchResponse } from './models';
export declare class SearchClient extends BaseClient {
    constructor(config?: Config, customHeaders?: Record<string, string>, verbose?: boolean);
    search(request: SearchRequest): Promise<SearchResponse>;
    webSearch(query: string, count?: number, needSummary?: boolean): Promise<SearchResponse>;
    webSearchWithSummary(query: string, count?: number): Promise<SearchResponse>;
    imageSearch(query: string, count?: number): Promise<SearchResponse>;
    advancedSearch(query: string, options?: {
        searchType?: 'web' | 'web_summary' | 'image';
        count?: number;
        needContent?: boolean;
        needUrl?: boolean;
        sites?: string;
        blockHosts?: string;
        needSummary?: boolean;
        timeRange?: string;
    }): Promise<SearchResponse>;
}
//# sourceMappingURL=client.d.ts.map