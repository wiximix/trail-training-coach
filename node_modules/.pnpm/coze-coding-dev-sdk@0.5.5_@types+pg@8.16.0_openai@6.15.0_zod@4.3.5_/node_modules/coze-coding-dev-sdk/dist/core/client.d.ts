import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios';
import { Config } from './config';
export declare abstract class BaseClient {
    protected config: Config;
    protected axiosInstance: AxiosInstance;
    protected verbose: boolean;
    constructor(config?: Config, customHeaders?: Record<string, string>, verbose?: boolean);
    protected request<T = any>(method: string, url: string, data?: any, config?: AxiosRequestConfig): Promise<T>;
    protected requestWithResponse<T = any>(method: string, url: string, data?: any, config?: AxiosRequestConfig): Promise<AxiosResponse<T>>;
    private sanitizeHeaders;
    private logRequest;
    private logResponse;
    protected sleep(ms: number): Promise<void>;
}
//# sourceMappingURL=client.d.ts.map