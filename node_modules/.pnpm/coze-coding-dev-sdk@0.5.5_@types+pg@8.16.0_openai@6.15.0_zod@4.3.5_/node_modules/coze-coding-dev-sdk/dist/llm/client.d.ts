import { AIMessageChunk } from '@langchain/core/messages';
import { Config } from '../core/config';
import { LLMConfig, Message, LLMResponse } from './models';
export declare class LLMClient {
    private config;
    constructor(config: Config);
    private createLLM;
    private convertMessages;
    private convertContent;
    stream(messages: Message[], llmConfig?: LLMConfig, previousResponseId?: string, extraHeaders?: Record<string, string>): AsyncGenerator<AIMessageChunk, void, unknown>;
    invoke(messages: Message[], llmConfig?: LLMConfig, previousResponseId?: string, extraHeaders?: Record<string, string>): Promise<LLMResponse>;
}
//# sourceMappingURL=client.d.ts.map