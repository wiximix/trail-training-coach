"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.asrCommand = exports.ttsCommand = void 0;
const commander_1 = require("commander");
const index_1 = require("../../index");
const chalk_1 = __importDefault(require("chalk"));
const ora_1 = __importDefault(require("ora"));
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const constants_1 = require("../constants");
const utils_1 = require("../utils");
exports.ttsCommand = new commander_1.Command('tts')
    .description('Text-to-speech conversion')
    .requiredOption('-t, --text <text>', 'Text to convert to speech')
    .option('--speaker <speaker>', 'Speaker voice to use', 'zh_female_xiaohe_uranus_bigtts')
    .option('--mock', 'Use mock mode (test run)')
    .option('-H, --header <header>', 'Custom HTTP header (format: "Key: Value" or "Key=Value", can be used multiple times)', (value, previous) => previous.concat([value]), [])
    .action(async (options) => {
    const spinner = (0, ora_1.default)('Converting text to speech...').start();
    try {
        const config = new index_1.Config();
        const customHeaders = {};
        const parsedHeaders = (0, utils_1.parseHeaders)(options.header);
        if (parsedHeaders) {
            Object.assign(customHeaders, parsedHeaders);
        }
        if (options.mock) {
            customHeaders[constants_1.RUN_MODE_HEADER] = constants_1.RUN_MODE_TEST;
            console.log(chalk_1.default.yellow('🧪 Mock mode enabled (test run)'));
        }
        const client = new index_1.TTSClient(config, Object.keys(customHeaders).length > 0 ? customHeaders : undefined);
        const response = await client.synthesize({
            uid: 'cli-user',
            text: options.text,
            speaker: options.speaker,
        });
        spinner.succeed(chalk_1.default.green('Text-to-speech conversion completed!'));
        console.log(chalk_1.default.cyan(`Audio URL: ${response.audioUri}`));
        console.log(chalk_1.default.cyan(`Audio Size: ${response.audioSize} bytes`));
    }
    catch (error) {
        spinner.fail(chalk_1.default.red('Text-to-speech conversion failed'));
        console.error(chalk_1.default.red(error.message));
        process.exit(1);
    }
});
exports.asrCommand = new commander_1.Command('asr')
    .description('Automatic speech recognition')
    .option('-u, --url <url>', 'Audio file URL')
    .option('-f, --file <path>', 'Local audio file path (will be base64 encoded)')
    .option('-H, --header <header>', 'Custom HTTP header (format: "Key: Value" or "Key=Value", can be used multiple times)', (value, previous) => previous.concat([value]), [])
    .option('--verbose', 'Enable verbose logging to print HTTP request details', false)
    .action(async (options) => {
    const spinner = (0, ora_1.default)('Recognizing speech...').start();
    try {
        if (!options.url && !options.file) {
            spinner.fail(chalk_1.default.red('Please provide either --url or --file'));
            process.exit(1);
        }
        const config = new index_1.Config();
        const customHeaders = (0, utils_1.parseHeaders)(options.header);
        const client = new index_1.ASRClient(config, customHeaders, options.verbose);
        const request = options.url
            ? { url: options.url }
            : { base64Data: fs.readFileSync(path.resolve(options.file)).toString('base64') };
        const result = await client.recognize(request);
        spinner.succeed(chalk_1.default.green('Speech recognition completed!'));
        console.log(chalk_1.default.cyan('\nRecognized text:'));
        console.log(result.text);
    }
    catch (error) {
        spinner.fail(chalk_1.default.red('Speech recognition failed'));
        console.error(chalk_1.default.red(error.message));
        process.exit(1);
    }
});
//# sourceMappingURL=voice.js.map