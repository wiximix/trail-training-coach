"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.imageCommand = void 0;
const commander_1 = require("commander");
const index_1 = require("../../index");
const chalk_1 = __importDefault(require("chalk"));
const ora_1 = __importDefault(require("ora"));
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const axios_1 = __importDefault(require("axios"));
const constants_1 = require("../constants");
const utils_1 = require("../utils");
exports.imageCommand = new commander_1.Command('image')
    .description('Generate images using AI')
    .requiredOption('-p, --prompt <text>', 'Image generation prompt')
    .option('-o, --output <path>', 'Output file path')
    .option('--size <size>', 'Image size (2K, 4K, or WIDTHxHEIGHT)', '2K')
    .option('--mock', 'Use mock mode (test run)')
    .option('-H, --header <header>', 'Custom HTTP header (format: "Key: Value" or "Key=Value", can be used multiple times)', (value, previous) => previous.concat([value]), [])
    .option('--verbose', 'Enable verbose logging to print HTTP request details', false)
    .action(async (options) => {
    const spinner = (0, ora_1.default)('Generating image...').start();
    try {
        const config = new index_1.Config();
        const customHeaders = {};
        const parsedHeaders = (0, utils_1.parseHeaders)(options.header);
        if (parsedHeaders) {
            Object.assign(customHeaders, parsedHeaders);
        }
        if (options.mock) {
            customHeaders[constants_1.RUN_MODE_HEADER] = constants_1.RUN_MODE_TEST;
            console.log(chalk_1.default.yellow('🧪 Mock mode enabled (test run)'));
        }
        const client = new index_1.ImageGenerationClient(config, Object.keys(customHeaders).length > 0 ? customHeaders : undefined, options.verbose);
        const result = await client.generate({
            prompt: options.prompt,
            size: options.size,
        });
        spinner.succeed(chalk_1.default.green('Image generated successfully!'));
        if (result.data && result.data.length > 0 && result.data[0].url) {
            console.log(chalk_1.default.cyan(`Image URL: ${result.data[0].url}`));
            if (options.output) {
                spinner.start('Downloading image...');
                const response = await axios_1.default.get(result.data[0].url, { responseType: 'arraybuffer' });
                const outputPath = path.resolve(options.output);
                fs.writeFileSync(outputPath, response.data);
                spinner.succeed(chalk_1.default.green(`Image saved to ${outputPath}`));
            }
        }
    }
    catch (error) {
        spinner.fail(chalk_1.default.red('Failed to generate image'));
        console.error(chalk_1.default.red(error.message));
        process.exit(1);
    }
});
//# sourceMappingURL=image.js.map