import { BaseClient } from '../core/client';
import { Config } from '../core/config';
import { ASRRequest, ASRResponse } from './models';
export declare class ASRClient extends BaseClient {
    constructor(config?: Config, customHeaders?: Record<string, string>, verbose?: boolean);
    /**
     * 识别音频文件中的语音内容
     *
     * 音频要求:
     * - 音频时长 ≤ 2小时
     * - 音频大小 ≤ 100MB
     * - 支持编码: WAV/MP3/OGG OPUS
     *
     * @param request - ASR 请求参数
     * @param request.uid - 用户唯一标识
     * @param request.url - 音频文件 URL (与 base64Data 二选一)
     * @param request.base64Data - Base64 编码的音频数据 (与 url 二选一)
     * @returns 识别结果，包含文本、时长和详细信息
     */
    recognize(request: ASRRequest): Promise<ASRResponse>;
}
//# sourceMappingURL=asr.d.ts.map